// Map Example
// Demonstrates map transformations on arrays

routes [
  template { path, method }
  
  map (item) = {
    path = "/api/" + item.path
    method = item.method
  }
  
  "users", "GET"
  "posts", "POST"
  "comments", "DELETE"
]
